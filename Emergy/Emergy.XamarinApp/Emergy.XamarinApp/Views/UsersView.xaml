<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Emergy.XamarinApp.ViewModels;assembly=Emergy.XamarinApp"
             x:Class="Emergy.XamarinApp.Views.UsersView"
             BackgroundColor="#e2dede" Title="Χρηστες" Icon="ic_group_black_24dp.png">
  <ContentPage.BindingContext>
    <viewModels:UserViewViewModel/>
  </ContentPage.BindingContext>
  <ContentPage.Content>
    <StackLayout VerticalOptions="Start">
      
      <Entry x:Name="UserEntry" Placeholder="Όνομα" />
      <Button BackgroundColor="Black" TextColor="White" Text="Αναζήτηση"/>
     
      <ListView ItemsSource="{Binding Users}" IsEnabled="False">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Text="{Binding Username}" Grid.Column="0" TextColor="#000000" HorizontalOptions="Start" VerticalOptions="Center"></Label>
                <Switch Grid.Column="1" HorizontalOptions="End" VerticalOptions="Start"  />
              </Grid>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      </StackLayout>
    
  </ContentPage.Content>
  
</ContentPage>